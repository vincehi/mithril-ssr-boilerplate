const m = require('mithril');
// const m = require('../common/m');

module.exports = {
    view: vnode => {
        return (
            <footer>
                <div>The Footer</div>
            </footer>
        )
    }
};
