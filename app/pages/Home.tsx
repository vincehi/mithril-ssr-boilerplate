import m from '../../lib/m';

const Home = {
  data: {
    title: 'Home'
  },
  view: () => {
    return (
      <div>
        Contenu de la page Homepage !
      </div>
    );
  }
};

export default Home;
