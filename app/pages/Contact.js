const m = require('mithril');
const Layout = require('../components/Layout')

module.exports = {
    oninit: vnode => {
    },

    view: vnode => {
        return (
                <main>
                    Contenu de la page Contact !
                </main>
        )
    }
};
