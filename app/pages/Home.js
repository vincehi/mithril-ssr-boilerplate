import m from '../common/m';

const Home = {
  data: {
    title: 'Home'
  },
  view: () => {
    return (
      <div>
        Contenu de la page Homepage !
      </div>
    );
  }
};

export default Home;
